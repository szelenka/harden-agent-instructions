[tox]
env_list = py312, lint, typecheck
min_version = 4.0

[testenv]
deps =
    pytest
    django
    pytest-django
commands = pytest {posargs}

[testenv:lint]
deps = ruff
commands = ruff check src/ tests/

[testenv:typecheck]
deps =
    mypy
    django-stubs
commands = mypy src/
