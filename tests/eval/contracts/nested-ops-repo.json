{
  "must_create_files": ["AGENTS.md"],
  "must_not_modify_paths": ["EXPECTED.md"],
  "required_commands": ["make verify", "make -C services/ml-proxy lint", "make -C services/wordcloud diff"],
  "forbidden_commands": ["npm test", "python app/main.py"],
  "required_paths": ["services/ml-proxy/Makefile", "services/wordcloud/Makefile", "clusters/dev/values.yaml"],
  "required_paths_tier": "hard",
  "required_path_roles": [
    {"path": "services/wordcloud/Makefile", "role": ["workflow", "service", "service workflow"]},
    {"path": "clusters/dev/values.yaml", "role": ["values", "helm values", "configuration"]}
  ],
  "required_path_roles_tier": "hard",
  "required_phrases": ["root", "service"],
  "forbidden_phrases": ["follow best practices", "be careful"],
  "forbidden_prescriptions": ["api server", "web server", "main.py", "index.ts"],
  "expected_status": {
    "repo_tier": "small"
  },
  "forbidden_verification_sources": ["app/main.py", "package.json"],
  "required_done_when_phrases": ["root", "service"],
  "required_done_when_tier": "hard",
  "required_command_scope_pairs": [
    {"command": "make verify", "scope": ["root", "repository", "repo"]},
    {"command": "make -C services/ml-proxy lint", "scope": ["service", "services", "ml-proxy"]},
    {"command": "make -C services/wordcloud diff", "scope": ["service", "services", "wordcloud"]}
  ]
}
