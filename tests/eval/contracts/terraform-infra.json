{
  "must_create_files": ["AGENTS.md"],
  "must_not_modify_paths": ["EXPECTED.md"],
  "required_commands": [["terraform"]],
  "forbidden_commands": ["npm", "python", "go", "make", "pip"],
  "required_paths": [],
  "required_phrases": ["terraform"],
  "forbidden_phrases": ["follow best practices", "be careful"],
  "forbidden_prescriptions": [],
  "expected_status": {
    "repo_tier": "small"
  }
}
